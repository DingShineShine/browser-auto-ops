# 企业多渠道店铺智能 RPA 部署开发方案

## 一、项目目标

建设一套面向企业内部多渠道店铺运营的智能 RPA 底层能力，用于自动执行店铺后台、数据平台、选品工具、广告平台等重复性任务。

典型任务包括：

- 店铺后台数据导出
- 卖家精灵、关键词、选品数据采集
- 多账号定时任务
- 报表下载
- 登录验证、人工辅助流程
- 任务失败追踪与重跑

本项目短期目标不是一次性建设完整 RPA 平台，而是先建设可稳定运行的 **单点 Agent + ADS 执行闭环**，再逐步演进为多执行节点调度系统。

---

## 二、部署方案对比

### 方案一：零散部署

每台 VPS 独立安装 Agent，Agent 拉取或接收任务后，直接调用本机浏览器实例执行任务。

#### 优点

- 架构简单，上手快。
- 每台机器直接控制本机浏览器，链路短。
- 浏览器环境真实，不容易触发风控。
- 可以复用现有通用浏览器 skills 或浏览器工具。

#### 缺点

- 每台 VPS 都需要单独安装、配置、升级、排错。
- 执行日志、截图、下载文件分散。
- 任务状态无法统一追踪。
- 出现登录、短信、人机验证等人工介入时，处理方式不统一。
- 多账号、多任务后维护成本高。

#### 适用场景

```text
临时任务
少量账号
PoC 验证
```

---

### 方案二：单点 Agent + ADS 集中执行

在一台 VPS 上安装：

```text
Agent
browser-auto-ops
AdsPower / ADS 指纹浏览器
多个浏览器 profile
```

任务在该 VPS 上串行或低并发执行。

#### 优点

- 比方案一更容易统一维护。
- 可以围绕业务流程自研 RPA skills。
- 可以逐步沉淀 trace、debug、下载、人工介入能力。
- 适合先跑通业务闭环。

#### 缺点

- 单台 VPS 有资源上限。
- ADS 稳定性需要验证。
- Agent 通过 ADS CDP 操作浏览器，多了一层依赖。
- 并发能力有限。

#### 适用场景

```text
第一阶段上线
串行任务
少量定时任务
单业务线试点
```

---

### 方案三：多执行节点 Agent + ADS

方案三是方案二的进化版本。

每台 VPS 都是一套执行环境：

```text
VPS Worker
  Agent / Runner
  browser-auto-ops
  AdsPower / ADS
  多个 profile
```

后续由统一任务中心分发任务到不同 VPS worker。

#### 优点

- 可以水平扩展。
- 多任务可以分发到多个 Agent + ADS 环境并行执行。
- 单个 VPS 故障不影响全部任务。
- 可以统一管理任务状态、执行记录、资源占用。

#### 缺点

- 需要任务中心和资源调度。
- 需要 profile lock、并发控制、worker 心跳。
- 平台化开发成本更高。

#### 适用场景

```text
多账号
多业务线
并发任务
规模化运营
```

---

## 三、推荐路线

不建议一开始直接建设完整方案三。

推荐路线：

```text
第一阶段：方案二
  单点 Agent + ADS
  串行任务
  先打通任务执行闭环

第二阶段：方案二增强
  定时任务
  人工介入
  执行追踪
  失败重跑

第三阶段：方案三
  多 Agent + ADS worker
  任务中心
  资源调度
  并发执行
```

也就是说：

```text
先让单点稳定，再做集群化。
```

---

## 四、核心开发模块

### 模块一：自研 RPA Skills / Browser Runtime

目标：提供稳定的浏览器自动化底层能力。

当前核心能力：

- `chrome-direct`：控制本机 Chrome。
- `ads`：控制 AdsPower profile。
- `bao daemon`：长驻运行时，减少重复连接。
- `bao state`：暴露页面可操作元素。
- `bao click/input/select/eval`：执行浏览器操作。
- `DownloadManager`：处理导出下载。
- `debug snapshot`：排查流程卡点。

短期重点：

- 提升 `ads` 模式稳定性。
- 完善复杂前端组件识别。
- 让 Agent 能稳定执行“选品导出”这类流程。

---

### 模块二：人工介入环节

该模块应优先建设，因为上线早期最容易导致任务失败的不是调度，而是：

- 登录失效
- 短信验证码
- 邮箱验证码
- 人机验证
- 风控提示
- 页面流程变化
- Agent 不确定下一步

如果没有人工介入，流程容易卡死。

#### 人工介入状态

任务执行中如果遇到不可自动处理的问题，进入：

```text
manual_required
```

#### 人工介入信息

系统应记录：

```text
task_id
当前页面 URL
当前截图
当前 state
需要人工处理的原因
人工处理说明
恢复按钮
```

#### 人工介入流程

```text
Agent 执行任务
  -> 遇到登录/验证码/风控
  -> 标记 manual_required
  -> 通知人工
  -> 人工进入浏览器处理
  -> 人工点击继续
  -> Agent 重新读取 state
  -> 继续任务
```

#### 一期目标

- 能暂停任务。
- 能截图说明原因。
- 人工处理后能继续。
- 失败原因可记录。

---

### 模块三：任务交互模块

该模块初期不建议叫“任务中心”，可以叫 **任务交互模块** 或 **任务执行接口**。

初期可以先写死定时任务，后续再演进为任务配置和任务拉取。

#### 阶段一：写死定时任务

适合 MVP。

例如：

```text
每天 10:00 执行卖家精灵选品导出
固定 profile
固定参数
固定下载目录
```

优点：

- 开发快
- 容易验证
- 不需要复杂任务系统

缺点：

- 灵活性差
- 每新增流程都要改代码或配置

#### 阶段二：配置化任务

任务参数配置化：

```json
{
  "task_type": "seller_sprite_product_export",
  "profile": "seller-ads-01",
  "params": {
    "market": "US",
    "month": "2026-06",
    "node_id": "3732831"
  },
  "schedule": "0 10 * * *"
}
```

#### 阶段三：任务拉取

Agent 定时从任务接口拉任务：

```text
GET /tasks/next?worker_id=xxx
POST /tasks/{id}/heartbeat
POST /tasks/{id}/status
POST /tasks/{id}/result
```

任务状态：

```text
pending
running
manual_required
success
failed
cancelled
retrying
```

---

### 模块四：执行追溯方案

执行追溯应前置建设，否则失败后无法判断问题。

每次任务应保存：

```text
state
action
screenshot
network
download
error
debug snapshot
```

建议目录：

```text
.bao/trace/{task_id}/
  events.jsonl
  states/
  screenshots/
  network/
  downloads/
  summary.json
```

#### 必须能回答的问题

任务失败时，需要能回答：

```text
失败在哪一步？
当时页面是什么？
Agent 看到了哪些元素？
Agent 点了哪个元素？
点击后页面有没有变化？
是否出现验证码？
是否下载了文件？
```

#### 一期目标

- 每个任务生成 trace。
- 每个关键步骤有截图。
- 每个失败有 error reason。
- 每个下载有文件路径。
- 可以按 task_id 回放关键过程。

---

### 模块五：任务中心 / 多 Agent + ADS 调度

该模块放到后期建设，用于解决规模化问题。

#### 功能

- 多 VPS worker 注册。
- worker 心跳。
- profile 资源池。
- profile lock。
- 并发任务分配。
- 失败重试。
- 任务查询。
- 任务重跑。

#### 任务分发逻辑

```text
任务中心
  -> 查找可用 worker
  -> 查找可用 ADS profile
  -> 下发任务
  -> worker 执行
  -> 回传状态和结果
```

#### 资源控制

```text
一个 profile 同一时间只能执行一个任务
一个 VPS 限制最大并发浏览器数量
任务结束后释放 profile lock
任务超时自动回收
```

---

## 五、一期开发排期建议

### 一期目标

先交付一个可用的单点 RPA 执行闭环。

范围：

```text
单 VPS
单 Agent / Runner
AdsPower profile
固定/半固定定时任务
人工介入
执行追溯
文件下载
```

### 建议周期

```text
3-4 周
```

不建议写 1 周。1 周只能算技术验证，不能算可上线 MVP。

---

### 第 1 周：底层浏览器能力与场景验证

内容：

- 完善 `ads` 模式。
- 打通 AdsPower profile 启动。
- 连接 CDP。
- 跑通卖家精灵选品导出流程。
- 完善 `bao state/click/downloads`。
- 形成 debug snapshot。

交付：

- 单 profile 可完成卖家精灵导出。
- 下载 Excel 到本地。
- 失败时能看到截图和 state。

---

### 第 2 周：人工介入与任务执行流程

内容：

- 增加任务状态模型。
- 增加 `manual_required`。
- 登录失效、验证码、异常页面时暂停任务。
- 人工处理后继续执行。
- 固定定时任务执行。

交付：

- 可跑固定任务。
- 遇到登录/验证码可暂停。
- 人工处理后可继续。

---

### 第 3 周：追溯与稳定性

内容：

- trace 日志。
- 步骤截图。
- action 记录。
- 下载记录。
- 错误原因。
- 失败重试。
- 任务 summary。

交付：

- 每个任务可查。
- 每个失败可定位。
- 支持重跑。

---

### 第 4 周：试点与验收

内容：

- 接入 1-2 个真实流程。
- 连续跑定时任务。
- 修复稳定性问题。
- 输出验收报告。

交付：

- 一套可试运行的 RPA MVP。
- 有明确成功率和失败原因。
- 能给业务方试用。

---

## 六、一期验收标准

建议一期验收标准如下：

1. 支持至少一个真实业务流程端到端执行，例如卖家精灵选品数据导出。

2. 支持 AdsPower profile 启动、连接、操作。

3. 支持定时任务执行。

4. 支持任务状态流转：

```text
pending -> running -> success / failed / manual_required
```

5. 支持人工介入：

```text
登录验证
短信验证码
邮箱验证码
人机验证
风控提示
```

6. 支持执行追溯：

```text
state
action
screenshot
error
download
summary
```

7. 支持下载文件落地并返回路径。

8. 支持失败重试和任务重跑。

---

## 七、迭代方向

### 二期

- 配置化任务。
- 多 profile。
- 任务查询。
- 任务重跑。
- 资源锁。
- 简单调度。

### 三期

- 多 VPS worker。
- 任务中心。
- 并发调度。
- 资源池。
- 权限管理。
- 监控看板。

### 四期

- 多平台流程模板。
- Workflow 编排。
- 业务人员配置任务。
- 数据回流系统。
- 报表自动生成。

---

## 八、给领导的总结

可以这样表述：

```text
当前不建议直接做大而全的 RPA 平台。

第一阶段先做单点 Agent + ADS 的 RPA 执行闭环，
重点解决浏览器控制、人工介入、任务追溯和下载结果落地。

等单点流程稳定后，再演进为多 Agent + ADS worker 的任务中心，
实现并发任务和多账号资源调度。
```

最终路线：

```text
方案二先落地
方案三再扩展
```

也就是：

```text
先串行稳定
再并行扩展
先执行闭环
再任务中心
```
